<!DOCTYPE html>
<html lang="id" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <meta name="description" content="<%= news.excerpt %>">
    <meta name="keywords" content="<%= news.tags.join(', ') %>">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <!-- AOS Animation Library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= news.title %>">
    <meta property="og:description" content="<%= news.excerpt %>">
    <meta property="og:image" content="<%= news.imageUrl %>">
    <meta property="og:type" content="article">
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= news.title %>">
    <meta name="twitter:description" content="<%= news.excerpt %>">
    <meta name="twitter:image" content="<%= news.imageUrl %>">
</head>
<body class="bg-white news-page">
    <%- include('partials/navbar') %>
    <!-- Breadcrumb -->
    <section class="pt-24 pb-8 bg-gray-50">
        <div class="container-custom">
            <nav class="flex" aria-label="Breadcrumb">
                <ol class="inline-flex items-center space-x-1 md:space-x-3">
                    <li class="inline-flex items-center">
                        <a href="/" class="inline-flex items-center text-sm font-medium text-gray-700 hover:text-primary">
                            <i class="fas fa-home mr-2"></i>
                            Home
                        </a>
                    </li>
                    <li>
                        <div class="flex items-center">
                            <i class="fas fa-chevron-right text-gray-400 mx-2"></i>
                            <a href="/news" class="ml-1 text-sm font-medium text-gray-700 hover:text-primary md:ml-2">News</a>
                        </div>
                    </li>
                    <li aria-current="page">
                        <div class="flex items-center">
                            <i class="fas fa-chevron-right text-gray-400 mx-2"></i>
                            <span class="ml-1 text-sm font-medium text-gray-500 md:ml-2"><%= news.category %></span>
                        </div>
                    </li>
                </ol>
            </nav>
        </div>
    </section>

    <!-- Article Header -->
    <section class="pb-8 bg-gray-50">
        <div class="container-custom">
            <div class="max-w-4xl mx-auto">
                <!-- Category and Reading Time -->
                <div class="flex items-center justify-between mb-6" data-aos="fade-up">
                    <span class="bg-primary text-white px-4 py-2 rounded-full text-sm font-medium">
                        <%= news.category %>
                    </span>
                    <div class="flex items-center text-gray-600 text-sm">
                        <i class="far fa-clock mr-2"></i>
                        <span><%= news.readTime %> read</span>
                    </div>
                </div>
                
                <!-- Title -->
                <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-black mb-6 leading-tight" data-aos="fade-up" data-aos-delay="200">
                    <%= news.title %>
                </h1>
                
                <!-- Author and Date -->
                <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-8 pb-8 border-b border-gray-200" data-aos="fade-up" data-aos-delay="400">
                    <div class="flex items-center mb-4 sm:mb-0">
                        <div class="w-12 h-12 bg-gradient-to-br from-primary to-cyber rounded-full flex items-center justify-center mr-4">
                            <span class="text-white font-bold text-lg"><%= news.author.charAt(0) %></span>
                        </div>
                        <div>
                            <p class="font-bold text-black"><%= news.author %></p>
                            <p class="text-gray-600 text-sm"><%= news.authorTitle %></p>
                        </div>
                    </div>
                    
                    <div class="flex items-center text-gray-600 text-sm">
                        <i class="far fa-calendar mr-2"></i>
                        <span><%= new Date(news.publishDate).toLocaleDateString('id-ID', { 
                            weekday: 'long',
                            year: 'numeric', 
                            month: 'long', 
                            day: 'numeric' 
                        }) %></span>
                    </div>
                </div>
                
                <!-- Tags -->
                <div class="flex flex-wrap gap-2 mb-8">
                    <% news.tags.forEach(function(tag) { %>
                    <span class="bg-gray-100 text-gray-700 px-3 py-1 rounded-full text-sm hover:bg-gray-200 transition-colors">
                        #<%= tag %>
                    </span>
                    <% }); %>
                </div>
            </div>
        </div>
    </section>

    <!-- Article Content -->
    <article class="section-padding bg-white">
        <div class="container-custom">
            <div class="max-w-4xl mx-auto">
                <!-- Featured Image -->
                <div class="relative overflow-hidden rounded-2xl mb-12 shadow-2xl" data-aos="fade-up">
                    <img src="<%= news.imageUrl %>" alt="<%= news.title %>" class="w-full h-64 md:h-96 object-cover">
                    <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
                </div>
                
                <!-- Article Body -->
                <div class="prose prose-lg max-w-none prose-headings:text-black prose-headings:font-bold prose-p:text-gray-700 prose-p:leading-relaxed prose-a:text-primary prose-a:no-underline hover:prose-a:underline prose-strong:text-black prose-ul:text-gray-700 prose-ol:text-gray-700 prose-li:text-gray-700" data-aos="fade-up" data-aos-delay="200">
                    <%- news.content %>
                </div>
                
                <!-- Social Share -->
                <div class="mt-12 pt-8 border-t border-gray-200">
                    <h3 class="text-xl font-bold text-black mb-4">Share this article</h3>
                    <div class="flex items-center space-x-4">
                        <a href="https://twitter.com/intent/tweet?text=<%= encodeURIComponent(news.title) %>&url=<%= encodeURIComponent('https://mbclaboratory.com/news/' + news.slug) %>" 
                           target="_blank" 
                           class="flex items-center px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors">
                            <i class="fab fa-twitter mr-2"></i>
                            Twitter
                        </a>
                        <a href="https://www.linkedin.com/sharing/share-offsite/?url=<%= encodeURIComponent('https://mbclaboratory.com/news/' + news.slug) %>" 
                           target="_blank"
                           class="flex items-center px-4 py-2 bg-blue-700 text-white rounded-lg hover:bg-blue-800 transition-colors">
                            <i class="fab fa-linkedin mr-2"></i>
                            LinkedIn
                        </a>
                        <a href="https://www.facebook.com/sharer/sharer.php?u=<%= encodeURIComponent('https://mbclaboratory.com/news/' + news.slug) %>" 
                           target="_blank"
                           class="flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                            <i class="fab fa-facebook mr-2"></i>
                            Facebook
                        </a>
                        <button onclick="copyToClipboard()" class="flex items-center px-4 py-2 bg-gray-600 text-white rounded-lg hover:bg-gray-700 transition-colors">
                            <i class="fas fa-link mr-2"></i>
                            Copy Link
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </article>

    <!-- Related Articles -->
    <% if (relatedNews.length > 0) { %>
    <section class="section-padding bg-gradient-to-br from-gray-50 to-white">
        <div class="container-custom">
            <div class="max-w-6xl mx-auto">
                <h2 class="text-3xl md:text-4xl font-bold text-black mb-12 text-center">
                    Related <span class="text-gradient">Articles</span>
                </h2>
                
                <div class="grid grid-cols-1 md:grid-cols-<%= Math.min(relatedNews.length, 3) %> gap-8">
                    <% relatedNews.forEach(function(article) { %>
                    <article class="bg-white rounded-2xl shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2 overflow-hidden border border-gray-100">
                        <div class="relative overflow-hidden h-48">
                            <img src="<%= article.imageUrl %>" alt="<%= article.title %>" class="w-full h-full object-cover transition-transform duration-300 hover:scale-105">
                            <div class="absolute top-4 left-4">
                                <span class="bg-primary text-white px-3 py-1 rounded-full text-sm font-medium">
                                    <%= article.category %>
                                </span>
                            </div>
                            <div class="absolute top-4 right-4">
                                <span class="bg-black/50 text-white px-2 py-1 rounded text-xs">
                                    <i class="far fa-clock mr-1"></i><%= article.readTime %>
                                </span>
                            </div>
                        </div>
                        
                        <div class="p-6">
                            <div class="flex items-center text-sm text-black/60 mb-3">
                                <span><%= new Date(article.publishDate).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                                <span class="mx-2">•</span>
                                <span><%= article.author %></span>
                            </div>
                            
                            <h3 class="text-xl font-bold text-black mb-3 line-clamp-2 hover:text-primary transition-colors">
                                <a href="/news/<%= article.slug %>"><%= article.title %></a>
                            </h3>
                            
                            <p class="text-black/70 mb-4 line-clamp-3">
                                <%= article.excerpt %>
                            </p>
                            
                            <a href="/news/<%= article.slug %>" class="text-primary hover:text-primary-600 font-medium text-sm flex items-center transition-colors">
                                Read More
                                <i class="fas fa-arrow-right ml-2 text-xs"></i>
                            </a>
                        </div>
                    </article>
                    <% }); %>
                </div>
                
                <div class="text-center mt-12">
                    <a href="/news" class="btn btn-primary">
                        <i class="fas fa-newspaper mr-2"></i>
                        View All News
                    </a>
                </div>
            </div>
        </div>
    </section>
    <% } %>

    <!-- CTA Section -->
    <section class="section-padding gradient-bg relative overflow-hidden">
        <div class="absolute inset-0">
            <div class="absolute top-1/2 left-1/4 w-64 h-64 bg-white/10 rounded-full blur-3xl"></div>
            <div class="absolute bottom-1/4 right-1/3 w-80 h-80 bg-mint/20 rounded-full blur-3xl"></div>
        </div>
        
        <div class="container-custom relative z-10">
            <div class="text-center max-w-4xl mx-auto">
                <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
                    Need Expert <span class="text-mint">Cybersecurity</span> Advice?
                </h2>
                <p class="text-xl text-white/90 mb-8 leading-relaxed">
                    Get personalized recommendations and solutions from our cybersecurity experts
                </p>
                
                <div class="flex flex-col sm:flex-row gap-4 justify-center">
                    <a href="/contact" class="btn bg-mint text-black hover:bg-mint-600 px-8 py-4 text-lg font-bold">
                        <i class="fas fa-phone-alt mr-2"></i>
                        Get Consultation
                    </a>
                    <a href="/services" class="btn btn-secondary bg-white text-primary border-2 border-white hover:bg-gray-100 px-8 py-4 text-lg font-bold">
                        <i class="fas fa-shield-alt mr-2"></i>
                        Our Services
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <%- include('partials/footer') %>

    <script>
        // Mobile menu toggle
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        mobileMenuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });

        // Copy to clipboard function
        function copyToClipboard() {
            const url = window.location.href;
            navigator.clipboard.writeText(url).then(function() {
                // Show feedback
                const button = event.target.closest('button');
                const originalText = button.innerHTML;
                button.innerHTML = '<i class="fas fa-check mr-2"></i>Copied!';
                button.classList.remove('bg-gray-600', 'hover:bg-gray-700');
                button.classList.add('bg-green-600', 'hover:bg-green-700');
                
                setTimeout(() => {
                    button.innerHTML = originalText;
                    button.classList.remove('bg-green-600', 'hover:bg-green-700');
                    button.classList.add('bg-gray-600', 'hover:bg-gray-700');
                }, 2000);
            });
        }

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Reading progress indicator
        window.addEventListener('scroll', () => {
            const article = document.querySelector('article');
            if (article) {
                const articleTop = article.offsetTop;
                const articleHeight = article.offsetHeight;
                const windowHeight = window.innerHeight;
                const scrollTop = window.pageYOffset;
                
                const progress = Math.min(100, Math.max(0, 
                    ((scrollTop - articleTop + windowHeight) / articleHeight) * 100
                ));
                
                // You can use this progress value to show a reading progress bar
                // For example: document.querySelector('.progress-bar').style.width = progress + '%';
            }
        });
    </script>
    
    <!-- AOS Animation JavaScript -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({
            duration: 800,
            easing: 'ease-in-out',
            once: true,
            offset: 100
        });
    </script>
</body>
</html>
